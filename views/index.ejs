<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ejs</title>
  </head>
  <body>
    <h1>My Website</h1>

    <%- include("partials/header") %>

    <h2><%= message %></h3>

    <h4> Today's Date is : <%= new Date().toDateString() %></h4>

    <ul>
      <% people.forEach(person => { %>
        <li> <%= person.name %> </li>
      <% }) %>
    </ul>

    <ul>
    <% people.forEach(person => {  %>
      
       <%  if (person.age > 30) {  %>
          <li> <%= person.major  %> </li>
       <%  }  %>

    <% }) %>
    </ul>
  </body>
</html>
